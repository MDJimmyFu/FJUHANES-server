def search_extracted():
    try:
        # We don't have the file yet, we need to save the response_payload in the script first
        # Let's modify extract_response.py to save the raw payload
        pass

if __name__ == "__main__":
    # Actually, let's just modify the extract_response.py to save the raw payload
    # and then search it
    pass
